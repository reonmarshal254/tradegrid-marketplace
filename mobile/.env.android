# Used by the Capacitor Android build: the app runs standalone, so the API
# must be an absolute URL. Google/Paystack keys come from the base .env.
VITE_API_URL=https://tradegrid-backend.onrender.com
